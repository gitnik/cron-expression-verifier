{
  "private": true,
  "name": "cron",
  "version": "1.0.0",
  "description": "A template for kick starting a Cloudflare Workers project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Nik Karbaum <n.karbaum@talentry.com>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  },
  "dependencies": {
    "@cloudflare/wrangler": "^1.9.2",
    "cron-parser": "^2.15.0",
    "html-loader": "^1.1.0"
  }
}
